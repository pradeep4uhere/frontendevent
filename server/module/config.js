/*
*Change the variables here only, do not change the KEY Name
*API_HOST_URL :  Url of the API where server will intract 
*
*/


var config;
config = {
     API_HOST_URL  :   'http://127.0.0.1:8000/api/front/en/v1',
}
module.exports = config;

